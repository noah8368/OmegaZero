# NF-001: Synthetic validation of the conditional flow + baselines

- **Status:** concluded — **H0 gate CLEARED**; early H2 read logged (see Interpretation)
- **Hypotheses:** [H0](../hypotheses.md#h0--the-implementation-is-correct-pre-req-not-a-research-claim) (correctness), [H2](../hypotheses.md#h2--which-conditional-model-is-best-flow-vs-qr-vs-mdn)
- **Started / Concluded:** 2026-08-08 / 2026-08-09
- **Code:** `research/experiments/nf001_synthetic.py` (flow/QR/MDN/unconditional × 4 generators; PIT/coverage/quantile-MAE)
- **Run:** `for s in 0 1 2; do python research/experiments/nf001_synthetic.py --generator hetero_gaussian lognormal bimodal skewnormal --model flow qr mdn unconditional --n 20000 --epochs 150 --seed $s --outdir research/figs/NF-001/seed_$s; done` @ SHA a3ec266. Outputs: `research/figs/NF-001/seed_{0,1,2}/` (summary.json + PIT/coverage plots).

## Smoke test (2026-08-08, n=4k, 40 epochs — NOT the real run)

Sanity only, to clear H0 plumbing. Flow and MDN recover oracle NLL to ~0.01–0.03 with
low PIT-KS; unconditional floor is ΔNLL≈+0.8 worse (→ real conditional signal exists);
QR sits a bit behind flow/MDN on calibration. QR's NLL is a *derived* quantity from its
quantile grid (coarse finite-difference) — indicative only; judge QR on pinball /
coverage / quantile-MAE. Full runs: larger n, more epochs, all 4 generators, seeds.

## Question

Before touching chess data: do our conditional-density implementations (NSF flow, QR,
MDN) and our calibration diagnostics (PIT, coverage) correctly recover *known*
conditional distributions — and does the flow show any advantage over the baselines on
distributions with the features we expect in eval error (skew, heavy tails,
heteroscedasticity)?

This is the H0 gate: if we can't recover ground truth we know, we can't trust anything
the models say about chess positions. It also gives an early read on H2 in a controlled
setting.

## Setup

**Synthetic data generators** (`u` given a 1-D or low-D conditioner `x`), ground-truth
CDF known in closed form so PIT/coverage/quantiles have an exact reference:

1. **Heteroscedastic Gaussian.** `u | x ~ N(μ(x), σ(x)²)` with σ growing in `x` (the
   "tactical positions are noisier" analogue). Sanity floor.
2. **Conditional log-normal.** Skewed, nonnegative, heavy right tail — matches `|error|`
   shape if we ever model absolute error.
3. **Conditional bimodal mixture.** Two Gaussians whose weights/means depend on `x`.
   Tests whether QR (which needn't assume unimodality) and the flow both cope; MDN
   should nail it, single-component models should visibly fail — a good discriminator.
4. **Skew-t / heavy-tailed.** Stress the tail quantiles (95th/99th) the search actually
   uses.

**Models under test** (shared conditioner MLP where possible, for a fair fight):
- Conditional NSF (zuko) — trained by max-likelihood.
- Quantile regression — pinball loss on a fixed quantile grid; non-crossing enforced.
- Mixture density network — Gaussian/log-normal mixture, NLL loss.
- Unconditional baseline — fit ignoring `x` (floor; conditional models must beat it).

**Metrics:**
- Held-out **NLL** vs the true generator's NLL (gap → estimation error).
- **PIT** histogram + a uniformity test (KS / chi-square) against Uniform(0,1).
- **Coverage** at q ∈ {0.5, 0.8, 0.9, 0.95, 0.99}, global and stratified by `x`-bin.
- **Quantile error**: predicted vs true q-quantile across the `x` range (the number that
  matters for search).
- Wall-clock train + per-sample inference time (early read on H5 feasibility).

**Reproducibility:** fixed seeds; every generator + config behind CLI flags; single
`nf001_synthetic.py` driver that writes plots to `research/figs/NF-001/` and a metrics
table to the run dir. Record git SHA + exact command in this file on completion.

## Results

Full runs: n=20k (val=5k), 150 epochs, 3 seeds (0/1/2), all 4 generators × 4 models.
Metrics below are **averaged across seeds** (per-seed numbers in `seed_*/summary.json`);
seed-to-seed spread was small and changed no ranking. ΔNLL is model NLL − oracle NLL
(closed-form generator); qMAE = quantile-MAE vs the true quantile.

| generator | model | ΔNLL | PIT-KS | qMAE@.95 | qMAE@.99 |
|---|---|---:|---:|---:|---:|
| hetero_gaussian | flow | +0.024 | 0.023 | 0.060 | 0.078 |
| | qr | −0.292* | 0.062 | 0.094 | 0.117 |
| | **mdn** | **+0.008** | 0.028 | **0.048** | **0.060** |
| | unconditional | +0.825 | 0.013 | 1.221 | 1.565 |
| lognormal | flow | +0.030 | 0.026 | 0.075 | 0.132 |
| | qr | −0.175* | 0.058 | 0.069 | 0.097 |
| | **mdn** | **+0.019** | 0.020 | 0.060 | 0.113 |
| | unconditional | +0.321 | 0.095 | 0.380 | 0.629 |
| bimodal | flow | +0.018 | 0.015 | 0.065 | 0.067 |
| | qr | −0.130* | 0.051 | 0.173 | 0.136 |
| | **mdn** | **+0.007** | 0.019 | **0.058** | **0.051** |
| | unconditional | +0.777 | 0.132 | 0.785 | 1.377 |
| skewnormal | flow | +0.020 | 0.038 | 0.044 | 0.055 |
| | qr | −0.273* | 0.059 | **0.029** | **0.036** |
| | **mdn** | **+0.008** | 0.027 | 0.029 | 0.043 |
| | unconditional | +1.281 | 0.011 | 1.261 | 1.597 |

\* **QR NLL is not comparable** — it is a finite-difference density derived from QR's
coarse quantile grid, so it dips *below* the oracle (impossible for a proper density).
Judge QR on PIT-KS / coverage / quantile-MAE only (as this file pre-registered). On those,
QR is the least-calibrated conditional model — except it wins the `skewnormal` **tails**,
its home turf (it optimizes quantiles directly).

## Interpretation

**H0 gate — CLEARED.** flow and MDN recover the known conditional distributions to
ΔNLL ≈ +0.007…+0.030 nats (near oracle), PIT-KS ≈ 0.015–0.038, near-nominal coverage, and
small tail qMAE (0.04–0.13). The unconditional floor is far worse exactly where it should
be (ΔNLL +0.32…+1.28; qMAE@.99 up to 1.6) — so the diagnostics correctly *detect* signal
when present and *punish* its removal. The density models + calibration diagnostics are
trustworthy; we can believe what they say on chess data.

**H2 (early, synthetic-only read) — no flow advantage; MDN is the quiet winner.** MDN is
at-least-as-good as the flow on **every** generator, on both ΔNLL and tail qMAE, including
the two cases meant to favor the flow (bimodal multimodality, skewnormal heavy tails) — and
it is cheaper (closed-form CDF, no transform inversion). This triggers the pre-registered
"flow ≈ others ⇒ simpler model is the deployment path" branch and supports the Aug-8
"best tool wins, flow not sacred" call.

Two honesty caveats: (1) the deck mildly favors MDN — `hetero_gaussian` and `bimodal` *are*
Gaussian mixtures (MDN's exact inductive bias); the genuinely non-Gaussian tests are
`lognormal`/`skewnormal`, where MDN still edges the flow but by less. (2) These targets are
benign — the skew-t may not be heavy-tailed *enough* and bimodal is only 2 modes. Real
chess eval-error may be nastier (sharp multimodality + fat tails + strong
heteroscedasticity), the regime where a flow could finally separate. **Prior, not verdict:**
carry MDN (primary) + QR (tail-specific margins) forward; keep the flow as a backstop, to be
re-tested on real error once the NNUE embedding exists. Concrete H2-stress design in
[NF-001b](NF-001b.md).

## Follow-ups

- If flow ≈ QR on all synthetic cases → strong prior that QR is the deployment path;
  keep the flow for the multimodal/heavy-tail cases where it may separate.
- NF-002: label + embedding pipeline (real positions, current eval) — the next file.
